name: Release Infer
on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release"
        required: true
      production:
        description: "Release to PyPI (not TestPyPI)"
        required: false
        default: "false"

defaults:
  run:
    shell: bash
    working-directory: infer

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
          - ubuntu-24.04-arm
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5

      - name: Setup binstall
        uses: cargo-bins/cargo-binstall@main

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          package_json_file: tools/package.json

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          # TODO: .node-versionなどをdatasetと共通化する
          node-version-file: tools/.node-version
          cache: "pnpm"
          cache-dependency-path: tools/pnpm-lock.yaml

      - name: Setup cargo-about
        run: cargo binstall cargo-about

      - name: Build wheel
        working-directory: tools
        run: |
          pnpm i
          pnpm run tools:build_e2k_py --wheel --version ${{ inputs.version }}

      - name: Build sdist
        working-directory: tools
        if: matrix.os == 'ubuntu-latest'
        run: |
          pnpm run tools:build_e2k_py --sdist --version ${{ inputs.version }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: e2k-py-${{ matrix.os }}
          path: |
            infer/target/wheels/*.whl
            infer/target/wheels/*.tar.gz

  publish:
    needs:
      - build

    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: write

    environment: release

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: e2k-py-*
          merge-multiple: true
          path: dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: ${{ inputs.production == 'true' && 'https://upload.pypi.org/legacy/' || 'https://test.pypi.org/legacy/' }}
          verbose: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.version }}
          name: v${{ inputs.version }}
          body: |
            Release v${{ inputs.version }}
          draft: false
          prerelease: ${{ inputs.production != "true" }}
